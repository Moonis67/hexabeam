import type {MetadataRoute} from "next";import {services} from "@/src/content/services";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const base="https://hexabeam.ae";const fixed=["","/services","/business-setup","/visas","/tax-accounting","/corporate-legal","/business-support","/logistics-concierge","/about","/contact","/privacy","/terms"];return[...fixed.map(url=>({url:`${base}${url}/`,changeFrequency:"monthly" as const,priority:url===""?1:.7})),...services.map(s=>({url:`${base}/services/${s.slug}/`,changeFrequency:"monthly" as const,priority:.8}))]}
